# تصميم MX2 كوكيل أدوات داخل MX1

## الهدف
يعمل MX2 كمساعد محادثة داخل MX1، لا كمربع نص فقط. يفهم الطلب، يحدد إن كان يحتاج معرفة عامة أو موصلًا خارجيًا، يعرض خطة قصيرة، ثم يستخدم الأدوات المصرح بها ضمن صلاحيات محددة. العمليات التي تغيّر البيانات أو تنشر أو تحذف لا تُنفّذ تلقائيًا.

## تجربة المستخدم
تبدأ الجلسة من حقل كتابة واحد مع أوضاع المحادثة، البرمجة، التشخيص، التخطيط، والتنفيذ. يفتح زر التطبيقات والتكاملات مركزًا موحدًا يحتوي على البحث والتصفية حسب الفئة، حالة كل موصل، نطاقات الصلاحيات، وزري بدء الربط أو إدارة الصلاحيات. توجد فئات للبحث، Google، نماذج الذكاء الاصطناعي، الوسائط و3D، الاتصال، الملفات، المشاريع، النشر والمراقبة.

## الصلاحيات
لكل موصل نطاقات واضحة مثل قراءة المستودعات أو قراءة البريد أو إنشاء مسودة فقط. تُعرض الصلاحيات قبل الربط. لا تُحفظ الأسرار في localStorage أو في الواجهة. في النسخة المحلية الحالية، أزرار بدء الربط تجهز طلب الموافقة ولا تفتح OAuth فعليًا، إلى أن تُضاف إعدادات OAuth/API الرسمية لكل مزود.

## التنفيذ الآمن
يبقى التحليل والقراءة والمعاينة منخفضة المخاطر. أما النشر، الحذف، تغيير الأسرار، إرسال البريد، أو تعديل موارد خارجية فيحتاج إلى تأكيد إداري منفصل. يجب تسجيل العملية والموصل والصلاحيات والنتيجة، مع نقطة تراجع عند الإمكان.

## حالة التنفيذ
أُنشئ فرع محلي باسم `feature/mx2-connector-hub`، وطُبقت رقعة على `mx1/worker.js` وأضافت كتالوجًا موسعًا يضم GitHub وCloudflare والمتصفح وGoogle Drive وGmail وGoogle Calendar وChatGPT/OpenAI وGemini وTripo AI وSlack وDiscord وNotion وDropbox وLinear وVercel وSentry، مع البحث والتصفية وحالات مرتبط/متاح/موقوف مؤقتًا. اجتاز الملف فحص صياغة Node و`git diff --check`.

## ما يتطلب إعدادًا لاحقًا
لكل مزود ربط حقيقي، يلزم تحديد طريقة المصادقة الرسمية، عنوان callback، client ID/secret أو API key، ونطاقات أقل صلاحية. لا ينبغي نشر هذه الرقعة قبل مراجعة المستخدم لها وتجربة المصادقة في بيئة معاينة.
