const headers = {
  "content-type": "application/json; charset=utf-8",
  "cache-control": "no-store",
  "access-control-allow-origin": "*",
  "access-control-allow-methods": "GET,POST,OPTIONS",
  "access-control-allow-headers": "content-type"
};

function json(data, status = 200) {
  return new Response(JSON.stringify(data), { status, headers });
}

async function schema(db) {
  await db.batch([
    db.prepare(`CREATE TABLE IF NOT EXISTS mx1_posts (id TEXT PRIMARY KEY, user_name TEXT NOT NULL, avatar TEXT NOT NULL, caption TEXT NOT NULL, image_url TEXT, likes INTEGER NOT NULL DEFAULT 0, created_at INTEGER NOT NULL)`),
    db.prepare(`CREATE TABLE IF NOT EXISTS mx1_comments (id TEXT PRIMARY KEY, post_id TEXT NOT NULL, user_name TEXT NOT NULL, body TEXT NOT NULL, created_at INTEGER NOT NULL)`),
    db.prepare(`CREATE TABLE IF NOT EXISTS mx2_sessions (id TEXT PRIMARY KEY, owner_key TEXT NOT NULL, title TEXT NOT NULL DEFAULT 'محادثة MX2', created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL)`),
    db.prepare(`CREATE TABLE IF NOT EXISTS mx2_messages (id TEXT PRIMARY KEY, session_id TEXT NOT NULL, role TEXT NOT NULL, content TEXT NOT NULL, mode TEXT NOT NULL DEFAULT 'chat', created_at INTEGER NOT NULL)`),
    db.prepare(`CREATE INDEX IF NOT EXISTS idx_mx2_messages_session ON mx2_messages(session_id, created_at)`),
    db.prepare(`CREATE TABLE IF NOT EXISTS deploy_projects (id TEXT PRIMARY KEY, name TEXT NOT NULL, source_type TEXT NOT NULL DEFAULT 'zip', source_url TEXT, status TEXT NOT NULL DEFAULT 'draft', created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL)`),
    db.prepare(`CREATE TABLE IF NOT EXISTS deploy_versions (id TEXT PRIMARY KEY, project_id TEXT NOT NULL, version TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'draft', preview_url TEXT, notes TEXT, created_at INTEGER NOT NULL)`),
    db.prepare(`CREATE TABLE IF NOT EXISTS deploy_actions (id TEXT PRIMARY KEY, project_id TEXT, version_id TEXT, action TEXT NOT NULL, status TEXT NOT NULL, details TEXT, created_at INTEGER NOT NULL)`)
  ]);
}

function id() { return crypto.randomUUID(); }
function safeText(value, max) { return String(value ?? "").trim().slice(0, max); }
async function sessionToken(secret, day) { const key = await crypto.subtle.importKey('raw', new TextEncoder().encode(secret), {name:'HMAC',hash:'SHA-256'}, false, ['sign']); const bytes = await crypto.subtle.sign('HMAC', key, new TextEncoder().encode('mx1:'+day)); return [...new Uint8Array(bytes)].map(b=>b.toString(16).padStart(2,'0')).join(''); }
async function isAuthorized(request, env) { if (!env.MX1_PASSWORD) return false; const match = (request.headers.get('Cookie') || '').match(/(?:^|; )mx1_session=([^;]+)/); if (!match) return false; const day = Math.floor(Date.now()/86400000); const current = await sessionToken(env.MX1_PASSWORD, day); const previous = await sessionToken(env.MX1_PASSWORD, day-1); return match[1] === current || match[1] === previous; }
function loginPage() { return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>دخول MX1</title><style>body{margin:0;min-height:100vh;display:grid;place-items:center;background:#000;color:#f5f5f5;font-family:Arial,"Noto Sans Arabic",sans-serif}.box{width:min(390px,calc(100% - 36px));background:#121212;border:1px solid #303030;border-radius:16px;padding:28px;box-sizing:border-box;text-align:right}.brand{font:700 32px Georgia,serif;background:linear-gradient(45deg,#fa7e1e,#d62976,#7b2cff);-webkit-background-clip:text;color:transparent;text-align:center;margin-bottom:10px}.hint{color:#aaa;font-size:13px;line-height:1.7}.box input{width:100%;box-sizing:border-box;background:#1d1d1d;border:1px solid #3a3a3a;border-radius:9px;color:#fff;padding:12px;margin:14px 0;font-size:16px}.box button{width:100%;border:0;border-radius:9px;padding:12px;background:#0095f6;color:#fff;font-weight:700;font-size:15px;cursor:pointer}.error{color:#ff7b86;min-height:22px;margin-top:10px;font-size:13px}</style></head><body><form class="box" id="login"><div class="brand">MX1</div><h2>تسجيل الدخول</h2><div class="hint">أدخل كلمة مرور قاعدة تطوير الكود للوصول إلى المنصة.</div><input id="password" type="password" autocomplete="current-password" placeholder="كلمة المرور" autofocus><button>دخول</button><div class="error" id="error"></div></form><script>document.getElementById('login').addEventListener('submit',async function(e){e.preventDefault();const error=document.getElementById('error');error.textContent='جارٍ التحقق…';const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:document.getElementById('password').value})});if(r.ok)location.reload();else{error.textContent='كلمة المرور غير صحيحة';document.getElementById('password').select()}})</script></body></html>`; }
function extractAIText(value) {
  const blocked = new Set(['reasoning_content','prompt','messages','usage','token_ids','logprobs','system_fingerprint','id','request_id','finish_reason','stop_reason']);
  if (typeof value === 'string') { const text = value.trim(); return /^chatcmpl[-_]/i.test(text) ? '' : text; }
  if (!value || typeof value !== 'object') return '';
  const preferred = ['result','choices','output','data','message','content','response','output_text','generated_text','text'];
  for (const key of preferred) if (value[key] !== undefined && !blocked.has(key)) { const found = extractAIText(value[key]); if (found) return found; }
  if (Array.isArray(value)) for (const item of value) { const found = extractAIText(item); if (found) return found; }
  for (const [key, item] of Object.entries(value)) { if (!blocked.has(key) && !preferred.includes(key)) { const found = extractAIText(item); if (found) return found; } }
  return '';
}

const landing = `<!doctype html>
<html lang="ar" dir="rtl">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>MX1 — مساحة اجتماعية</title>
<style>
:root{--bg:#000;--card:#000;--ink:#f5f5f5;--muted:#a8a8a8;--line:#262626;--accent:#d62976;--accent2:#fa7e1e;--blue:#0095f6}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Arial,"Noto Sans Arabic",sans-serif}
.top{height:62px;background:#000;border-bottom:1px solid var(--line);position:sticky;top:0;z-index:5}.topin{max-width:980px;height:100%;margin:auto;display:flex;align-items:center;justify-content:space-between;padding:0 18px}.brand{font-family:Georgia,serif;font-size:27px;font-weight:700;letter-spacing:-1px}.brand span{background:linear-gradient(45deg,var(--accent2),var(--accent),#7b2cff);-webkit-background-clip:text;color:transparent}.actions{display:flex;gap:9px;align-items:center}.btn{border:0;border-radius:9px;padding:9px 15px;font-weight:700;cursor:pointer}.primary{background:var(--blue);color:#fff}.ghost{background:#262626;color:var(--ink)}body.layer-open{position:fixed;width:100%;overflow:hidden;overscroll-behavior:none}.admin-panel{display:none;position:fixed;inset:0;background:rgba(0,0,0,.96);z-index:30;padding:0}.admin-panel .admin-card{width:100%;min-height:100%;max-width:none;margin:0;border-radius:0;overflow:auto}.admin-card{max-width:620px;margin:60px auto;background:#121212;border:1px solid #363636;border-radius:14px;padding:22px;box-shadow:0 12px 50px #000}.admin-card h3{margin:0 0 8px}.admin-card p{color:var(--muted);font-size:13px}.admin-card input,.admin-card textarea{width:100%;background:#1c1c1c;border:1px solid #363636;border-radius:8px;color:var(--ink);padding:11px;margin:7px 0 12px;font-family:inherit}.admin-card textarea{min-height:130px;resize:vertical}.admin-actions{display:flex;gap:9px;justify-content:flex-start}.options-panel{display:none;position:fixed;inset:0;background:rgba(0,0,0,.96);z-index:40;padding:0;overflow:auto}.options-panel .options-card{width:100%;min-height:100%;max-width:none;margin:0;border-radius:0}.options-card{max-width:980px;margin:20px auto;background:#0d0d0d;border:1px solid #303030;border-radius:14px;padding:22px}.options-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px;margin-top:18px}.option-card{background:#171717;border:1px solid #2b2b2b;border-radius:12px;padding:15px}.option-card b{display:block;margin-bottom:7px}.option-card p{color:var(--muted);font-size:13px;line-height:1.6;margin:0 0 12px}.option-card button{margin:3px}.cf-panel{display:none;position:fixed;inset:0;background:rgba(0,0,0,.96);z-index:35;padding:0;overflow:auto}.cf-panel .cf-card{width:100%;min-height:100%;max-width:none;margin:0;border-radius:0}.cf-card{max-width:980px;margin:20px auto;background:#0d0d0d;border:1px solid #303030;border-radius:14px;padding:22px}.cf-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:10px;margin-top:18px}.cf-item{background:#171717;border:1px solid #2b2b2b;border-radius:10px;padding:13px}.cf-item b{display:block;margin-bottom:5px}.cf-item small{color:var(--muted);line-height:1.5}.cf-item a{display:inline-block;color:#42a5f5;margin-top:9px;font-size:12px;text-decoration:none}.cf-status{color:#68d391;font-size:12px}.mx2-panel{display:none;position:fixed;inset:0;background:rgba(0,0,0,.92);z-index:21;padding:0;overflow:hidden;overscroll-behavior:none;height:100dvh;min-height:100dvh}.mx2-panel.mx2-fullscreen{padding:0;background:#0d0d0d;height:100dvh;min-height:100dvh}.mx2-panel.mx2-fullscreen .mx2-card{width:100%;height:100%;max-width:none;border-radius:0}.mx2-card{width:100%;max-width:none;height:100dvh;min-height:100dvh;max-height:100dvh;margin:auto;background:#0d0d0d;border:0;border-radius:0;display:flex;flex-direction:column;overflow:hidden}.mx2-head{display:flex;align-items:center;justify-content:space-between;padding:16px 18px;border-bottom:1px solid #262626}.mx2-messages{flex:1 1 auto;min-height:0;overflow-y:auto;overflow-x:hidden;padding:18px;display:flex;flex-direction:column;gap:10px;overscroll-behavior:contain;-webkit-overflow-scrolling:touch}.mx2-msg{max-width:82%;padding:11px 13px;border-radius:14px;line-height:1.7;font-size:14px;white-space:pre-wrap}.mx2-user{align-self:flex-start;background:#262626}.mx2-ai{align-self:flex-end;background:#123b55}.mx2-input{flex:0 0 auto;display:flex;gap:8px;padding:13px;padding-bottom:calc(13px + env(safe-area-inset-bottom));border-top:1px solid #262626;background:#0d0d0d}.mx2-input textarea{flex:1;min-width:0;max-height:140px;overflow-y:auto;background:#1c1c1c;color:var(--ink);border:1px solid #363636;border-radius:9px;padding:10px;resize:none;font-family:inherit;font-size:16px;line-height:1.5}
.layout{max-width:980px;margin:28px auto;display:grid;grid-template-columns:minmax(0,620px) 280px;gap:30px;direction:ltr}.main,.side{direction:rtl}.side{position:sticky;top:88px;height:max-content}.profile{background:#000;border:1px solid var(--line);border-radius:12px;padding:18px;margin-bottom:18px}.profile h2{font-size:18px;margin:0 0 7px}.profile p{color:var(--muted);font-size:13px;line-height:1.7;margin:0}.links{display:flex;flex-direction:column;gap:10px;margin-top:15px}.links a{color:var(--ink);text-decoration:none;font-size:13px}.links a:hover{color:var(--blue)}
.composer{background:var(--card);border:1px solid var(--line);border-radius:12px;padding:16px;margin-bottom:18px}.composer-row{display:flex;gap:12px;align-items:flex-start}.avatar{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;color:#fff;font-weight:700;background:linear-gradient(135deg,#833ab4,#fd1d1d,#fcb045);flex:none}.composer textarea{width:100%;border:0;outline:0;resize:none;font-size:15px;padding:7px 0;min-height:60px;font-family:inherit}.composer-foot{display:flex;justify-content:space-between;align-items:center;border-top:1px solid #262626;padding-top:10px}.hint{font-size:12px;color:var(--muted)}
.post{background:#000;border:1px solid var(--line);border-radius:12px;margin-bottom:18px;overflow:hidden}.post-head{display:flex;align-items:center;gap:10px;padding:13px 15px}.user{font-weight:700;font-size:14px}.time{color:var(--muted);font-size:11px;margin-top:3px}.post-image{width:100%;aspect-ratio:1.35;background:linear-gradient(135deg,#171717,#262626);display:grid;place-items:center;color:#a1a1aa;font-size:52px}.post-image img{width:100%;height:100%;object-fit:cover}.post-body{padding:12px 15px}.post-actions{display:flex;gap:18px;align-items:center;margin-bottom:9px}.icon{border:0;background:transparent;font-size:22px;cursor:pointer;padding:0}.likes{font-weight:700;font-size:13px;margin-bottom:8px}.caption{font-size:14px;line-height:1.7}.empty{text-align:center;padding:45px 20px;color:var(--muted);background:#000;border:1px dashed var(--line);border-radius:12px}.toast{position:fixed;bottom:22px;left:50%;transform:translateX(-50%);background:#171717;color:#fff;padding:12px 18px;border-radius:22px;display:none;font-size:13px;z-index:9}
@media(max-width:760px){.layout{display:block;margin:18px 10px}.side{display:none}.topin{padding:8px 10px;display:block}.top{height:auto}.top .brand{font-size:24px;margin-bottom:8px}.top .actions{max-width:100%;overflow-x:auto;display:flex;flex-wrap:nowrap;padding-bottom:2px}.top .actions .btn{flex:0 0 auto;padding:10px 12px;font-size:13px}.mx2-head{padding:12px;gap:8px;align-items:flex-start}.mx2-head .actions{flex-wrap:wrap;justify-content:flex-end}.mx2-input{flex-wrap:wrap;padding:10px}.mx2-input select{width:100%;min-height:42px}.mx2-input textarea{min-width:0;width:100%;min-height:72px}.mx2-input>div{width:100%;flex-direction:row!important;align-items:center;justify-content:space-between}.mx2-input>div .btn{min-width:110px;min-height:42px}.mx2-messages{padding:12px}.mx2-msg{max-width:94%;font-size:14px}.options-card,.cf-card,.admin-card{padding:14px}.options-grid{grid-template-columns:1fr}.admin-actions{flex-wrap:wrap}.admin-card input,.admin-card textarea{font-size:16px}.studioAction{min-width:0}}
</style></head>
<body><header class="top"><div class="topin"><div class="brand"><span>MX1</span></div><div class="actions"><button class="btn ghost" onclick="loadFeed()">تحديث</button><button class="btn ghost" onclick="openOptions()">الخيارات</button><button class="btn ghost" onclick="openMX2()">MX2 AI</button><button class="btn ghost" onclick="openCloudflare()">Cloudflare</button><button class="btn ghost" onclick="openAdmin()">إدارة النشر</button><button class="btn primary" onclick="document.getElementById('caption').focus()">منشور جديد</button></div></div></header>
<div id="mx2" class="mx2-panel"><div class="mx2-card"><div class="mx2-head"><div><b>MX2 — المساعد الذكي</b><div class="hint">محادثة وبرمجة داخلية محفوظة محليًا</div></div><div class="actions"><button class="btn ghost" onclick="toggleMX2Fullscreen()">ملء الشاشة</button><button class="btn ghost" onclick="closeMX2()">إغلاق</button></div></div><div id="mx2Messages" class="mx2-messages"></div><div class="mx2-input"><select id="mx2Mode" style="background:#1c1c1c;color:var(--ink);border:1px solid #363636;border-radius:8px;padding:8px"><option value="chat">محادثة</option><option value="code">برمجة المنصة</option><option value="debug">تشخيص خطأ</option><option value="plan">خطة تنفيذ</option><option value="execute">تنفيذ تلقائي</option></select><textarea id="mx2Input" rows="2" placeholder="اكتب سؤالك أو طلب التعديل…"></textarea><div style="display:flex;flex-direction:column;gap:5px"><button id="mx2Send" class="btn primary" onclick="sendMX2()">إرسال</button><span id="mx2Status" class="hint">جاهز</span></div></div></div></div><div id="admin" class="admin-panel"><div class="admin-card"><h3>لوحة نشر MX1</h3><p>تُحفظ المسودة محليًا في هذا المتصفح. مفاتيح GitHub وCloudflare لا تُحفظ هنا ولا تُعرض للمستخدمين.</p><label>رسالة التحديث</label><input id="deployMessage" value="Update MX1 from admin panel"><label>محتوى التحديث الاختياري</label><textarea id="deployContent" placeholder="اكتب مسودة أو تعليمات التحديث…"></textarea><label>رمز الإدارة</label><input id="adminToken" type="password" placeholder="يُرسل عبر اتصال مشفر فقط"><div class="admin-actions"><button class="btn primary" onclick="publishProject()">نشر إلى GitHub ثم Cloudflare</button><button class="btn ghost" onclick="closeAdmin()">إغلاق</button></div><div id="deployStatus" class="hint" style="margin-top:12px"></div><hr style="border-color:#303030;margin:22px 0"><h3>استوديو المواقع</h3><p>أنشئ مشروعًا لموقع جاهز، ثم حلّله وافحصه وأنشئ معاينته قبل النشر. الإصدارات لا تستبدل بعضها تلقائيًا.</p><input id="studioName" placeholder="اسم الموقع"><input id="studioSource" placeholder="رابط GitHub أو اتركه فارغًا لرفع ZIP لاحقًا"><div class="admin-actions"><button class="btn ghost" onclick="studioCreate()">إنشاء مشروع</button><button class="btn ghost" onclick="studioList()">عرض المشاريع</button></div><div style="display:flex;gap:8px;margin-top:10px"><select id="studioAction" style="flex:1;background:#1c1c1c;color:var(--ink);border:1px solid #363636;border-radius:8px;padding:9px"><option value="analyze">تحليل</option><option value="validate">فحص</option><option value="preview">معاينة</option><option value="publish">نشر</option><option value="rollback">رجوع لإصدار</option></select><button class="btn primary" onclick="studioExecute()">تنفيذ</button></div><div id="studioStatus" class="hint" style="margin-top:12px"></div></div></div><div id="options" class="options-panel"><div class="options-card"><div style="display:flex;justify-content:space-between;align-items:center"><div><h2 style="margin:0">خيارات MX2</h2><p class="hint">لوحة موحدة للمحادثة والبرمجة والتنفيذ والتكاملات</p></div><button class="btn ghost" onclick="closeOptions()">إغلاق</button></div><div class="options-grid"><div class="option-card"><b>أوضاع الذكاء الاصطناعي</b><p>محادثة، برمجة المنصة، تشخيص الأخطاء، وخطط التنفيذ.</p><button class="btn primary" onclick="closeOptions();openMX2()">فتح MX2</button></div><div class="option-card"><b>الجلسات والسجل</b><p>سجل محادثات محلي وسحابي في D1 مع جلسات قابلة للاسترجاع.</p><button class="btn ghost" onclick="closeOptions();openMX2()">إدارة المحادثة</button></div><div class="option-card"><b>التنفيذ الآمن</b><p>تحليل وفحص ومعاينة ونشر ورجوع مع تأكيد للعمليات الحساسة.</p><button class="btn ghost" onclick="closeOptions();openAdmin()">استوديو المواقع</button></div><div class="option-card"><b>التكاملات</b><p>GitHub وCloudflare Workers وD1 وPages وR2 وKV وWorkers AI.</p><button class="btn ghost" onclick="closeOptions();openCloudflare()">عرض التكاملات</button></div><div class="option-card"><b>الملفات والمواقع</b><p>إدارة مصادر GitHub ومشاريع المواقع الجاهزة والإصدارات والمعاينات.</p><button class="btn ghost" onclick="closeOptions();openAdmin()">فتح المشاريع</button></div><div class="option-card"><b>الأمان</b><p>بوابة كلمة المرور، جلسة Cookie آمنة، وعدم عرض الأسرار في المتصفح.</p><span class="cf-status">مفعّل</span></div></div></div></div><div id="cloudflare" class="cf-panel"><div class="cf-card"><h2>لوحة Cloudflare الشاملة</h2><p class="hint">كتالوج الخدمات المرتبطة بـ MX1. القراءة متاحة هنا، أما الإنشاء والتعديل والحذف فيتطلب صلاحية إدارية وتأكيدًا منفصلًا.</p><div id="cfGrid" class="cf-grid"></div><div style="margin-top:18px"><button class="btn ghost" onclick="closeCloudflare()">إغلاق</button></div></div></div><div class="layout"><main class="main"><section class="composer"><div class="composer-row"><div class="avatar">م</div><textarea id="caption" maxlength="600" placeholder="ما الذي تريد مشاركته؟"></textarea></div><div class="composer-foot"><span class="hint">واجهة عامة — لا تحتاج إلى API key أو عنوان IP</span><button class="btn primary" onclick="publish()">نشر</button></div></section><section id="feed"><div class="empty">جارٍ تحميل المنشورات…</div></section></main>
<aside class="side"><div class="profile"><h2>MX1</h2><p>مساحتك الاجتماعية البسيطة، مع وصول مباشر من الواجهة وقاعدة البيانات محمية خلف الخادم.</p><div class="links"><a href="https://github.com/moha1-ma/Gobi" target="_blank" rel="noreferrer">مشروع Gobi على GitHub</a><a href="https://github.com/moha1-ma/72815917" target="_blank" rel="noreferrer">نسخة GitHub المرتبطة</a><a href="https://dash.cloudflare.com/" target="blank" rel="noreferrer">لوحة Cloudflare</a></div></div></aside></div><div id="toast" class="toast"></div>
<script>
const feed=document.getElementById('feed');
function toast(t){const x=document.getElementById('toast');x.textContent=t;x.style.display='block';setTimeout(()=>x.style.display='none',2600)}
function esc(t){return String(t||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function ago(ts){const m=Math.max(1,Math.floor((Date.now()-ts)/60000));return m<60?'منذ '+m+' د':m<1440?'منذ '+Math.floor(m/60)+' س':'منذ '+Math.floor(m/1440)+' ي'}
function card(p){const image=p.image_url?'<img src="'+esc(p.image_url)+'" alt="منشور MX1">':'<span>✦</span>';return '<article class="post"><div class="post-head"><div class="avatar">'+esc((p.user_name||'م')[0])+'</div><div><div class="user">'+esc(p.user_name||'مستخدم MX1')+'</div><div class="time">'+ago(p.created_at)+'</div></div></div><div class="post-image">'+image+'</div><div class="post-body"><div class="post-actions"><button class="icon" onclick=\"likePost(&quot;'+p.id+'&quot;,this)\">♡</button><button class="icon" onclick="document.getElementById(&quot;caption&quot;).focus()">◌</button><button class="icon" onclick="navigator.clipboard?.writeText(location.href);toast(&quot;تم نسخ الرابط&quot;)">⌁</button></div><div class="likes" id="likes-'+p.id+'">'+(p.likes||0)+' إعجاب</div><div class="caption"><b>'+esc(p.user_name||'مستخدم MX1')+'</b> '+esc(p.caption)+'</div></div></article>'}
async function loadFeed(){try{const r=await fetch('/api/feed');const d=await r.json();feed.innerHTML=d.posts?.length?d.posts.map(card).join(''):'<div class="empty">لا توجد منشورات بعد. كن أول من يشارك.</div>'}catch(e){feed.innerHTML='<div class="empty">تعذر تحميل المنشورات حاليًا.</div>'}}
async function publish(){const box=document.getElementById('caption');const caption=box.value.trim();if(!caption)return toast('اكتب نص المنشور أولًا');try{const r=await fetch('/api/posts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({caption,userName:'مستخدم MX1'})});const d=await r.json();if(!r.ok)throw Error(d.error);box.value='';toast('تم نشر المنشور');loadFeed()}catch(e){toast(e.message||'تعذر النشر')}}
async function likePost(id,button){button.disabled=true;try{const r=await fetch('/api/posts/'+id+'/like',{method:'POST'});const d=await r.json();if(d.likes!==undefined)document.getElementById('likes-'+id).textContent=d.likes+' إعجاب';button.textContent='♥';button.style.color='#ed4956'}finally{button.disabled=false}}
function showLayer(id){['mx2','admin','options','cloudflare'].forEach(x=>{const el=document.getElementById(x);if(el){el.style.display=x===id?'block':'none';if(x==='mx2'&&id!=='mx2')el.classList.remove('mx2-fullscreen')}});if(document.fullscreenElement&&id!=='mx2')document.exitFullscreen?.().catch(()=>{});document.body.classList.add('layer-open')}function closeLayer(id){const el=document.getElementById(id);if(el)el.style.display='none';if(!['mx2','admin','options','cloudflare'].some(x=>document.getElementById(x)?.style.display==='block'))document.body.classList.remove('layer-open')}function openOptions(){showLayer('options')}function closeOptions(){closeLayer('options')}
const cloudflareServices=[['Workers','التنفيذ والنشر والمسارات والإصدارات','مرتبط — mx1','workers'],['D1','قاعدة البيانات العلاقية','مرتبط — binding D','d1'],['KV','التخزين السريع بالمفاتيح','جاهز للربط','kv'],['R2','تخزين الملفات والكائنات','جاهز للربط','r2'],['Pages','استضافة الواجهات والنشر','متاح','pages'],['DNS','النطاقات والسجلات','متاح','dns'],['Routes','ربط المسارات بالـ Workers','متاح','routes'],['Queues','طوابير المهام','متاح','queues'],['Durable Objects','حالة متسقة وجلسات','متاح','durable-objects'],['Workers AI','نماذج الذكاء الاصطناعي','متاح','workers-ai'],['AI Gateway','بوابة النماذج والمراقبة','متاح','ai-gateway'],['Vectorize','البحث المتجهي','متاح','vectorize'],['Images','تحسين الصور والتحويل','متاح','images'],['Stream','الفيديو والبث','متاح','stream'],['Analytics','تحليلات الويب وWorkers','متاح','analytics'],['Logs & Tail','السجلات والتتبع','متاح','logs'],['Cache','التخزين المؤقت والتنقية','متاح','cache'],['WAF','جدار حماية التطبيقات','متاح','waf'],['Rate Limiting','حدود الطلبات','متاح','rate-limiting'],['Access','التحكم بالوصول','متاح','access'],['Tunnels','الأنفاق والاتصال الخاص','متاح','tunnels'],['Queues & Workflows','المهام الخلفية وسير العمل','متاح','workflows'],['Webhooks','الإشعارات والأحداث','متاح','webhooks'],['Email Routing','توجيه البريد','متاح','email'],['Load Balancing','توزيع الحمل','متاح','load-balancing'],['Browser Rendering','التصيير الآلي','متاح','browser-rendering'],['R2 Data Catalog','فهرسة البيانات','متاح','r2-catalog'],['Secrets Store','حفظ الأسرار','متاح','secrets-store']];function openCloudflare(){showLayer('cloudflare');const g=document.getElementById('cfGrid');g.innerHTML=cloudflareServices.map(s=>'<div class="cf-item"><b>'+s[0]+'</b><small>'+s[1]+'</small><div class="cf-status">'+s[2]+'</div><a href="https://dash.cloudflare.com/" target="_blank" rel="noreferrer">فتح في Cloudflare</a></div>').join('');document.getElementById('cloudflare').style.display='block'}function closeCloudflare(){closeLayer('cloudflare')}
const mx2Key='mx2-chat-history',mx2OwnerKey='mx2-owner-key',mx2SessionKey='mx2-session-id';let mx2Busy=false;function toggleMX2Fullscreen(){const panel=document.getElementById('mx2');panel.classList.toggle('mx2-fullscreen');if(panel.classList.contains('mx2-fullscreen')){document.documentElement.requestFullscreen?.().catch(()=>{})}else if(document.fullscreenElement){document.exitFullscreen?.().catch(()=>{})}}function mx2Fetch(url,options,timeout=60000){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeout);return fetch(url,{...options,signal:controller.signal}).finally(()=>clearTimeout(timer))}
function ownerKey(){let k=localStorage.getItem(mx2OwnerKey);if(!k){k=crypto.randomUUID();localStorage.setItem(mx2OwnerKey,k)}return k}async function ensureMX2Session(){let sid=localStorage.getItem(mx2SessionKey);if(sid)return sid;try{const r=await fetch('/api/chat/sessions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({ownerKey:ownerKey(),title:'محادثة MX2'})});const d=await r.json();if(r.ok&&d.session){sid=d.session.id;localStorage.setItem(mx2SessionKey,sid);return sid}}catch(e){}return ''}async function saveMX2Cloud(role,content,mode){const sid=await ensureMX2Session();if(!sid)return;try{await fetch('/api/chat/messages',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({ownerKey:ownerKey(),sessionId:sid,role,content,mode})})}catch(e){}}async function loadMX2Cloud(){const sid=localStorage.getItem(mx2SessionKey);if(!sid)return;try{const r=await fetch('/api/chat/sessions/'+encodeURIComponent(sid)+'?ownerKey='+encodeURIComponent(ownerKey()));const d=await r.json();if(r.ok&&Array.isArray(d.messages)){localStorage.setItem(mx2Key,JSON.stringify(d.messages.map(m=>({role:m.role,content:m.content,mode:m.mode}))));renderMX2()}}catch(e){}}function openMX2(){showLayer('mx2');renderMX2();loadMX2Cloud();document.getElementById('mx2Input').focus()}function closeMX2(){closeLayer('mx2')}function renderMX2(){const box=document.getElementById('mx2Messages');const h=JSON.parse(localStorage.getItem(mx2Key)||'[]');box.innerHTML=h.length?h.map(m=>'<div class="mx2-msg '+(m.role==='user'?'mx2-user':'mx2-ai')+'">'+esc(m.content)+'</div>').join(''):'<div class="empty">ابدأ محادثة جديدة مع MX2.</div>';box.scrollTop=box.scrollHeight}async function autoExecuteMX2(){const projectId=localStorage.getItem(studioProjectKey);if(!projectId)return 'لم يتم اختيار مشروع للنشر.';const results=[];for(const action of ['analyze','validate','preview']){const r=await mx2Fetch('/api/studio/execute',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({projectId,action})},30000);const d=await r.json();if(!r.ok)throw Error(d.error||'فشل '+action);results.push(action)}return 'اكتملت تلقائيًا: '+results.join('، ')+'؛ النشر الإنتاجي يحتاج تأكيدًا.'}async function sendMX2(){if(mx2Busy)return;const input=document.getElementById('mx2Input'),send=document.getElementById('mx2Send'),status=document.getElementById('mx2Status');const text=input.value.trim();if(!text)return;mx2Busy=true;send.disabled=true;status.textContent='جارٍ التحليل…';const mode=document.getElementById('mx2Mode').value;const h=JSON.parse(localStorage.getItem(mx2Key)||'[]');h.push({role:'user',content:text,mode});localStorage.setItem(mx2Key,JSON.stringify(h.slice(-20)));saveMX2Cloud('user',text,mode);input.value='';renderMX2();try{const r=await mx2Fetch('/api/ai',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({mode,messages:h.slice(-12)})});const d=await r.json();if(!r.ok)throw Error(d.error||'تعذر الاتصال بالمساعد');let answer=d.response||d.result||d.text||'لم تصل إجابة.';if(mode==='execute'){try{status.textContent='جارٍ تنفيذ الخطوات…';answer+='\\n\\n'+await autoExecuteMX2()}catch(execError){answer+='\\n\\nتعذر إكمال التنفيذ: '+execError.message}}const next=JSON.parse(localStorage.getItem(mx2Key)||'[]');next.push({role:'assistant',content:answer,mode});localStorage.setItem(mx2Key,JSON.stringify(next.slice(-20)));saveMX2Cloud('assistant',answer,mode);status.textContent='تمت الإجابة';renderMX2()}catch(e){const next=JSON.parse(localStorage.getItem(mx2Key)||'[]');const error=e.name==='AbortError'?'انتهت مهلة الاتصال، أعد المحاولة.':'تعذر الاتصال بالمساعد: '+e.message;next.push({role:'assistant',content:error,mode});localStorage.setItem(mx2Key,JSON.stringify(next.slice(-20)));saveMX2Cloud('assistant',error,mode);status.textContent='تعذر التنفيذ';renderMX2()}finally{mx2Busy=false;send.disabled=false}}document.addEventListener('keydown',e=>{if(e.key==='Escape'){const p=document.getElementById('mx2');if(p?.classList.contains('mx2-fullscreen'))toggleMX2Fullscreen()}if(e.key==='Enter'&&e.ctrlKey&&document.activeElement?.id==='mx2Input'){e.preventDefault();sendMX2()}})
const studioProjectKey='mx1-studio-project';async function studioCreate(){const name=document.getElementById('studioName').value.trim();const sourceUrl=document.getElementById('studioSource').value.trim();const status=document.getElementById('studioStatus');if(!name)return status.textContent='اكتب اسم المشروع أولًا';status.textContent='جارٍ إنشاء المشروع…';const r=await fetch('/api/studio/projects',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,sourceType:sourceUrl?'github':'zip',sourceUrl})});const d=await r.json();if(!r.ok)return status.textContent=d.error||'تعذر إنشاء المشروع';localStorage.setItem(studioProjectKey,d.project.id);status.textContent='تم إنشاء المشروع: '+d.project.name}async function studioList(){const status=document.getElementById('studioStatus');const r=await fetch('/api/studio/projects');const d=await r.json();status.textContent=r.ok?(d.projects||[]).map(p=>p.name+' — '+p.status).join(' | ')||'لا توجد مشاريع':'تعذر جلب المشاريع'}async function studioExecute(){const projectId=localStorage.getItem(studioProjectKey);const action=document.getElementById('studioAction').value;const status=document.getElementById('studioStatus');if(!projectId)return status.textContent='أنشئ مشروعًا أولًا';if((action==='publish'||action==='rollback')&&!confirm('هذه العملية تغيّر حالة النشر. هل تريد المتابعة؟'))return;status.textContent='جارٍ تنفيذ '+action+'…';const headers={'content-type':'application/json'};if(action==='publish'||action==='rollback')headers['x-mx1-confirm']='confirm';const r=await fetch('/api/studio/execute',{method:'POST',headers,body:JSON.stringify({projectId,action})});const d=await r.json();status.textContent=d.action?.details||d.error||'اكتملت العملية'}
const draftKey='mx1-admin-draft';function openAdmin(){showLayer('admin');const d=JSON.parse(localStorage.getItem(draftKey)||'{}');deployMessage.value=d.message||deployMessage.value;deployContent.value=d.content||''}function closeAdmin(){closeLayer('admin')}function saveDraft(){localStorage.setItem(draftKey,JSON.stringify({message:deployMessage.value,content:deployContent.value}))}async function publishProject(){saveDraft();const status=document.getElementById('deployStatus');status.textContent='جارٍ إرسال طلب النشر…';try{const r=await fetch('/api/publish',{method:'POST',headers:{'content-type':'application/json','authorization':'Bearer '+document.getElementById('adminToken').value},body:JSON.stringify({message:deployMessage.value,content:deployContent.value})});const d=await r.json();status.textContent=d.message||d.error||'تمت العملية';if(r.ok)toast('تم إرسال النشر')}catch(e){status.textContent='تعذر الاتصال بخدمة النشر'}}
loadFeed();
</script></body></html>`;

async function handleApi(request, env, url) {
  await schema(env.D);
  if (url.pathname === '/api/feed' && request.method === 'GET') {
    const rows = await env.D.prepare('SELECT id,user_name,avatar,caption,image_url,likes,created_at FROM mx1_posts ORDER BY created_at DESC LIMIT 50').all();
    return json({ posts: rows.results || [] });
  }
  if (url.pathname === '/api/posts' && request.method === 'POST') {
    const body = await request.json().catch(() => ({}));
    const caption = safeText(body.caption, 600);
    if (!caption) return json({error:'اكتب نص المنشور أولًا'}, 400);
    const post = { id:id(), user_name:safeText(body.userName, 40) || 'مستخدم MX1', avatar:'', caption, image_url:safeText(body.imageUrl, 1000) || null, likes:0, created_at:Date.now() };
    await env.D.prepare('INSERT INTO mx1_posts(id,user_name,avatar,caption,image_url,likes,created_at) VALUES(?,?,?,?,?,?,?)').bind(post.id,post.user_name,post.avatar,post.caption,post.image_url,post.likes,post.created_at).run();
    return json({post}, 201);
  }
  if (url.pathname === '/api/chat/sessions' && request.method === 'POST') {
    const body = await request.json().catch(() => ({}));
    const ownerKey = safeText(body.ownerKey, 120);
    if (!ownerKey) return json({error:'معرّف المالك مطلوب'}, 400);
    const now = Date.now();
    const session = {id:id(), owner_key:ownerKey, title:safeText(body.title, 120) || 'محادثة MX2', created_at:now, updated_at:now};
    await env.D.prepare('INSERT INTO mx2_sessions(id,owner_key,title,created_at,updated_at) VALUES(?,?,?,?,?)').bind(session.id,session.owner_key,session.title,session.created_at,session.updated_at).run();
    return json({session}, 201);
  }
  if (url.pathname === '/api/chat/sessions' && request.method === 'GET') {
    const ownerKey = safeText(url.searchParams.get('ownerKey'), 120);
    if (!ownerKey) return json({sessions:[]});
    const rows = await env.D.prepare('SELECT id,title,created_at,updated_at FROM mx2_sessions WHERE owner_key=? ORDER BY updated_at DESC LIMIT 50').bind(ownerKey).all();
    return json({sessions:rows.results || []});
  }
  const sessionMatch = url.pathname.match(/^\/api\/chat\/sessions\/([^/]+)$/);
  if (sessionMatch && request.method === 'GET') {
    const ownerKey = safeText(url.searchParams.get('ownerKey'), 120);
    const session = await env.D.prepare('SELECT id,title,created_at,updated_at FROM mx2_sessions WHERE id=? AND owner_key=?').bind(sessionMatch[1],ownerKey).first();
    if (!session) return json({error:'الجلسة غير موجودة'}, 404);
    const rows = await env.D.prepare('SELECT id,role,content,mode,created_at FROM mx2_messages WHERE session_id=? ORDER BY created_at ASC LIMIT 200').bind(session.id).all();
    return json({session,messages:rows.results || []});
  }
  if (sessionMatch && request.method === 'DELETE') {
    const ownerKey = safeText(url.searchParams.get('ownerKey'), 120);
    const session = await env.D.prepare('SELECT id FROM mx2_sessions WHERE id=? AND owner_key=?').bind(sessionMatch[1],ownerKey).first();
    if (!session) return json({error:'الجلسة غير موجودة'}, 404);
    await env.D.batch([env.D.prepare('DELETE FROM mx2_messages WHERE session_id=?').bind(session.id),env.D.prepare('DELETE FROM mx2_sessions WHERE id=?').bind(session.id)]);
    return json({deleted:true});
  }
  if (url.pathname === '/api/chat/messages' && request.method === 'POST') {
    const body = await request.json().catch(() => ({}));
    const ownerKey = safeText(body.ownerKey, 120), sessionId = safeText(body.sessionId, 80), content = safeText(body.content, 12000);
    const role = body.role === 'assistant' ? 'assistant' : 'user';
    if (!ownerKey || !sessionId || !content) return json({error:'بيانات الرسالة ناقصة'}, 400);
    const session = await env.D.prepare('SELECT id FROM mx2_sessions WHERE id=? AND owner_key=?').bind(sessionId,ownerKey).first();
    if (!session) return json({error:'الجلسة غير موجودة'}, 404);
    const message = {id:id(),session_id:sessionId,role,content,mode:safeText(body.mode,20)||'chat',created_at:Date.now()};
    await env.D.batch([env.D.prepare('INSERT INTO mx2_messages(id,session_id,role,content,mode,created_at) VALUES(?,?,?,?,?,?)').bind(message.id,message.session_id,message.role,message.content,message.mode,message.created_at),env.D.prepare('UPDATE mx2_sessions SET updated_at=? WHERE id=?').bind(message.created_at,sessionId)]);
    return json({message}, 201);
  }
  if (url.pathname === '/api/studio/projects' && request.method === 'GET') {
    const rows = await env.D.prepare('SELECT id,name,source_type,source_url,status,created_at,updated_at FROM deploy_projects ORDER BY updated_at DESC LIMIT 50').all();
    return json({projects:rows.results || []});
  }
  if (url.pathname === '/api/studio/projects' && request.method === 'POST') {
    const body = await request.json().catch(() => ({}));
    const name = safeText(body.name, 120), sourceType = safeText(body.sourceType, 20) || 'zip', sourceUrl = safeText(body.sourceUrl, 1000) || null;
    if (!name) return json({error:'اسم المشروع مطلوب'}, 400);
    const now = Date.now(), project = {id:id(), name, source_type:sourceType, source_url:sourceUrl, status:'draft', created_at:now, updated_at:now};
    await env.D.prepare('INSERT INTO deploy_projects(id,name,source_type,source_url,status,created_at,updated_at) VALUES(?,?,?,?,?,?,?)').bind(project.id,project.name,project.source_type,project.source_url,project.status,project.created_at,project.updated_at).run();
    return json({project}, 201);
  }
  if (url.pathname === '/api/studio/versions' && request.method === 'POST') {
    const body = await request.json().catch(() => ({}));
    const projectId = safeText(body.projectId, 80), notes = safeText(body.notes, 1000), version = safeText(body.version, 60) || 'v'+Date.now();
    if (!projectId) return json({error:'معرّف المشروع مطلوب'}, 400);
    const project = await env.D.prepare('SELECT id FROM deploy_projects WHERE id=?').bind(projectId).first();
    if (!project) return json({error:'المشروع غير موجود'}, 404);
    const item = {id:id(),project_id:projectId,version,status:'draft',preview_url:null,notes,created_at:Date.now()};
    await env.D.prepare('INSERT INTO deploy_versions(id,project_id,version,status,preview_url,notes,created_at) VALUES(?,?,?,?,?,?,?)').bind(item.id,item.project_id,item.version,item.status,item.preview_url,item.notes,item.created_at).run();
    return json({version:item}, 201);
  }
  if (url.pathname === '/api/studio/execute' && request.method === 'POST') {
    const body = await request.json().catch(() => ({}));
    const action = safeText(body.action, 30), projectId = safeText(body.projectId, 80), versionId = safeText(body.versionId, 80);
    const allowed = new Set(['analyze','validate','preview','publish','rollback']);
    if (!allowed.has(action)) return json({error:'العملية غير مسموحة'}, 400);
    if ((action === 'publish' || action === 'rollback') && request.headers.get('x-mx1-confirm') !== 'confirm') return json({error:'تأكيد إداري مطلوب لهذه العملية'}, 409);
    const status = action === 'publish' ? 'queued' : 'completed';
    const details = action === 'analyze' ? 'تم تجهيز تحليل بنية الموقع.' : action === 'validate' ? 'تم تجهيز فحص الملفات والإعدادات.' : action === 'preview' ? 'تم تسجيل طلب المعاينة.' : action === 'publish' ? 'تم تسجيل طلب النشر؛ يلزم ربط مصدر الملفات بـ Pages أو R2 لإتمام الرفع.' : 'تم تسجيل طلب الرجوع للإصدار.';
    const row = {id:id(),project_id:projectId||null,version_id:versionId||null,action,status,details,created_at:Date.now()};
    await env.D.prepare('INSERT INTO deploy_actions(id,project_id,version_id,action,status,details,created_at) VALUES(?,?,?,?,?,?,?)').bind(row.id,row.project_id,row.version_id,row.action,row.status,row.details,row.created_at).run();
    return json({action:row}, action === 'publish' ? 202 : 200);
  }
  if (url.pathname === '/api/ai' && request.method === 'POST') {
    if (!env.AI) return json({error:'Workers AI غير مهيأ لهذا Worker بعد.'}, 503);
    const body = await request.json().catch(() => ({}));
    const history = Array.isArray(body.messages) ? body.messages.slice(-12) : [];
    const mode = safeText(body.mode, 20) || 'chat';
    const modeGuide = mode === 'code' ? 'وضع برمجة المنصة: حلّل الطلب، حدّد الملفات والمسارات المتأثرة، اكتب خطة ورقعة كود دقيقة، ثم اقترح اختبارات. لا تدّعِ تنفيذ التعديل أو النشر ما لم يتم تأكيده.' : mode === 'debug' ? 'وضع التشخيص: حدّد السبب الجذري، خطوات إعادة الإنتاج، الإصلاح الأقل خطورة، واختبارات التحقق.' : mode === 'plan' ? 'وضع التخطيط: اكتب خطة تنفيذ مرحلية مع المخاطر والاعتماديات ونقطة التراجع.' : mode === 'execute' ? 'وضع التنفيذ التلقائي: حوّل الطلب إلى خطوات صغيرة، نفّذ التحليل والفحص والمعاينة تلقائيًا، تحقق من كل نتيجة، أعد المحاولة عند الفشل، ولا تنفذ الحذف أو النشر الإنتاجي أو تغيير الأسرار دون تأكيد إداري.' : 'وضع المحادثة: أجب بوضوح واختصار.';
    const architecture = 'بنية MX1 الحالية: Cloudflare Worker باسم mx1، واجهة عربية داكنة، D1 binding باسم D للمنشورات، Workers AI binding باسم AI، كتالوج Cloudflare داخل الواجهة، ومستودع GitHub هو moha1-ma/Gobi. يجب الحفاظ على الميزات الحالية وعدم كشف الأسرار أو عناوين قواعد البيانات. عمليات الحذف والنشر تحتاج تأكيدًا إداريًا.';
    const system = 'أنت MX2، مهندس برمجيات داخل منصة MX1. '+modeGuide+' '+architecture+' عند طلب تعديل برمجي، استخدم أسماء ملفات ومسارات واضحة، وقدّم كودًا قابلًا للمراجعة، ولا تنفذ أوامر خطرة أو تغيّر الإنتاج دون تأكيد.';
    const messages = [{role:'system',content:system}, ...history.map(m => ({role:m.role === 'assistant' ? 'assistant' : 'user', content:safeText(m.content, 4000)}))];
    const aiRoutes = ['@cf/zai-org/glm-4.7-flash','@cf/meta/llama-3.1-8b-instruct','@cf/meta/llama-3.2-3b-instruct','@cf/mistral/mistral-7b-instruct-v0.2','@cf/google/gemma-7b-it','@cf/qwen/qwen1.5-14b-chat-awq','@cf/deepseek-ai/deepseek-math-7b-instruct','@cf/ibm-granite/granite-4.0-h-micro','@cf/aisingapore/gemma-sea-lion-v4-27b-it','@cf/openchat/openchat-3.5-0106'];
    const failures=[];
    for (const route of aiRoutes) {
      try {
        const result = await Promise.race([env.AI.run(route, {messages}), new Promise((_, reject) => setTimeout(() => reject(new Error('timeout')), 12000))]);
        const response = extractAIText(result);
        if (response) return json({response});
        failures.push(route+':empty');
      } catch (e) { failures.push(route+':failed'); }
    }
    return json({error:'تعذر الحصول على إجابة حاليًا. أعد المحاولة بعد قليل.'}, 503);
  }
  if (url.pathname === '/api/publish' && request.method === 'POST') {
    if (!env.GITHUB_TOKEN || !env.ADMIN_TOKEN) return json({error:'خدمة النشر غير مهيأة بعد. أضف أسرار GITHUB_TOKEN وADMIN_TOKEN إلى Worker.'}, 503);
    const auth = request.headers.get('authorization') || '';
    if (auth !== 'Bearer ' + env.ADMIN_TOKEN) return json({error:'غير مصرح'}, 401);
    return json({message:'تم قبول طلب النشر. اربط سير GitHub Actions لإكمال النشر إلى Cloudflare.'});
  }
  const match = url.pathname.match(/^\/api\/posts\/([^/]+)\/like$/);
  if (match && request.method === 'POST') {
    await env.D.prepare('UPDATE mx1_posts SET likes=likes+1 WHERE id=?').bind(match[1]).run();
    const row = await env.D.prepare('SELECT likes FROM mx1_posts WHERE id=?').bind(match[1]).first();
    return json({likes: row?.likes || 0});
  }
  return json({error:'غير موجود'}, 404);
}

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (request.method === 'OPTIONS') return new Response(null, {status:204, headers});
    if (url.pathname === '/api/auth/login' && request.method === 'POST') {
      const body = await request.json().catch(() => ({}));
      if (!env.MX1_PASSWORD || safeText(body.password, 200) !== env.MX1_PASSWORD) return json({error:'كلمة المرور غير صحيحة'}, 401);
      const token = await sessionToken(env.MX1_PASSWORD, Math.floor(Date.now()/86400000));
      return new Response(JSON.stringify({ok:true}), {status:200, headers:{...headers,'Set-Cookie':`mx1_session=${token}; Max-Age=86400; Path=/; HttpOnly; Secure; SameSite=Lax`,'content-type':'application/json; charset=utf-8'}});
    }
    if (url.pathname.startsWith('/api/')) { if (!(await isAuthorized(request, env))) return json({error:'تسجيل الدخول مطلوب'}, 401); return handleApi(request, env, url); }
    if (!(await isAuthorized(request, env))) return new Response(loginPage(), {status:200, headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}});
    return new Response(landing, {headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}});
  }
};
